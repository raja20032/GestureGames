opencv-python>=4.2.0.34
# TensorFlow: this project uses TF1-style graphs/sessions via tf.compat.v1, which works on TF2 as well.
# On native Windows installs, the most reliable pip option is TF 2.10.x.
tensorflow==2.10.1; platform_system=="Windows"
tensorflow>=1.15.2; platform_system!="Windows"
# TF 2.10.x expects NumPy < 1.24
numpy>=1.20,<1.24
Flask>=2.0
keyboard>=0.13
pyKey
